create table unit_transaction (
	id serial,
	unit_id int,
	transaction_type int,
	troop_change int,
	location_id varchar(50), 
	created_on timestamp,
	modified_on timestamp,
	primary key(id)
	);
	
	create index unit_unittrans on unit_transaction(unit_id,id);